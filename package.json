{
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.2.4",
    "prettier-plugin-svelte": "^3.1.2",
    "turbo": "^1.11.3"
  },
  "packageManager": "pnpm@8.14.2",
  "engines": {
    "node": ">=18"
  }
}